# Engine

This directory contains the compiled `mat2biosignal` C++ binary used by the web backend.

Expected binary path:

- `engine/mat2biosignal`

The backend executes this binary to convert uploaded MATLAB `.mat` files into CSV/TXT outputs and packages the results into a ZIP archive.
